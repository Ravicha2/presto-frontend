# To home button

- Logo on top left will be use to go back to landing page/dashboard instead of a generic word 'back' button

# hidden element border

- showing all border of element all the time looks annoying, therefore we make all border only show up when click.

# Cursor Pointer

- cursor will change on what can It interact with objects such as move, scale ,and select

# Cancel, Exit modal

- All modal can be cancel by clicking outside of modal, not just cancel button

# Upload Image

- The upload image/thumbnail buttons are not default "choose file" button but rather big beautiful area (it not support drag and drop though)

# Choosing colours

- rather than typing hex number, use color input for less technical user, eyedrop also available

# Bring to Front/Send to Back

- when we do presentation we want order of element shown on slide, bring to front and send to back as implemented for that, done by right click